## 1. The rule in the published specs

- [x] 1.1 MODIFY `layered-configuration`'s *Configuration is layered into a corpus contract and swappable profiles*, reproducing all seven published scenarios verbatim; verify the delta's scenario titles match the published ones exactly, since a MODIFIED block that omits one is an archive-blocking error. *Checked mechanically rather than by eye: a script parsed both files and reported 7/7 reproduced.*
- [x] 1.2 Add the two classification paragraphs after the retrieval-settings paragraph — a setting that changes the bytes handed to the embedder is contract, including the identity of whatever produces them; and the contract's coverage claim holds in both directions; verify they sit with the other two classification paragraphs and before the precedence paragraph
- [x] 1.3 Add the scenario *Every setting that changes what is embedded is in the contract*; verify the other two requirements in that capability are left untouched because nothing in them becomes false. *The profile layer still has exactly one exception — the embedder — because a setting of this kind is not a profile setting at all.*
- [x] 1.4 ADD to `chunking-and-embedding` the requirement *What is embedded for a chunk is exactly the chunk's text*, with its two scenarios; verify it states that the embedder's asymmetric prefix is not an exception and that a heading path is recorded but not embedded

## 2. The embed-input tripwire

- [x] 2.1 Add `_RecordingEmbedder` to `tests/test_embedding.py`, subclassing `DeterministicEmbedder` so `name` and therefore corpus identity are unchanged; verify it records each `embed_documents` call and delegates
- [x] 2.2 Add the test, wiring through `build_context(config, embedder=..., gate=gate)` over the `sectioned_corpus` fixture; verify the expectation is recomputed with `chunk_text` from each document's `extracted_text` rather than read back from the store. *The embedder's width is taken from `config.contract.embed_dimensions` rather than from `conftest`'s constant, which `test_embedding.py` does not import.*
- [x] 2.3 Assert the sorted recorded texts equal the sorted recomputed ones, with a message naming the rule; verify the message says folding anything in is corpus-coupled and belongs in the contract
- [x] 2.4 Add both vacuity guards — the recorded list is non-empty, and at least one recomputed piece carries a non-empty heading path; verify the second fails if the fixture is swapped for the single-passage `corpus`. *Swapped and watched: `AssertionError: no chunk in this corpus carries a heading path … assert 0`. The fixture choice is load-bearing, not decorative.*
- [x] 2.5 Watch the test fail: temporarily fold the heading path into embed input at `services/ingestion.py:381` and run `pytest tests/test_embedding.py -x -q`; verify the failure names the rule, then restore and re-run green. *1 failed, 13 passed with the defect in; the message read "what reaches the embedder must be the chunk's own text and nothing else … This test failing is the signal to declare that value, not to update the test." Restored: 14 passed.*

## 3. The measurement guard

- [x] 3.1 Add `"corpus": context.corpus_fingerprint` as the first key of the `conditions` dict in `scripts/evaluate_retrieval.py`; verify it is read from the context rather than recomposed, so it is the identity the store enforces
- [x] 3.2 Add `REQUIRED_CONDITIONS = ("corpus",)` beside `conditions_match`, with the comment explaining why an absent key is a mismatch for this key and not for the others; verify `conditions_match` compares `corpus` first and reports a baseline that records none
- [x] 3.3 Print `Corpus:` on its own line in `print_report` rather than inside the comma-joined conditions line; verify `embedder=deterministic` and `reranker=none` still appear on that line, which two existing tests assert. *Both still pass; the identity is 158 characters and would have made the line unreadable.*
- [x] 3.4 Annotate `docs/retrieval-baseline.json` with the corpus identity those figures were produced under; verify no metric changes and the identity matches the default pinned at `tests/test_config.py:436-439`. *Annotated, not re-measured. `git log --since=2026-08-28 -S…` is empty for both `intfloat/multilingual-e5-large` and `chunk_overlap_chars`, the `fastembed==0.8.0` pin landed 2026-08-26, and `corpus_fingerprint(Contract(), "model")` printed the annotated string exactly.*
- [x] 3.5 Add `corpus` to `_conditions()` in `tests/test_evaluate_retrieval.py` and the three new tests — a baseline stating no corpus, a baseline over another corpus, and the shipped baseline stating one; verify `test_matching_conditions_compare_cleanly` still passes. *47 passed in that module.*
- [x] 3.6 MODIFY `retrieval-evaluation`'s *A measurement is comparable against a recorded baseline* and *A retrieval-quality claim names what it was measured on*, reproducing every published scenario title; verify comparability is stated over corpus identity and that a claim names the corpus first. *2/2 and 3/3 reproduced, checked by the same script as 1.1.*
- [x] 3.7 Run the harness end to end offline — `python scripts/evaluate_retrieval.py --embedder deterministic`; verify a `Corpus:` line appears and a `corpus:` line is among the reported differences, then delete the baseline's `corpus` key temporarily and verify the "baseline records none" message. *Both observed. With the key deleted the difference read `corpus: run '…|embedder=deterministic', baseline records none — a baseline that does not state the corpus it measured cannot be shown to be comparable`. Exit 0 either way: an incomparable run reports rather than fails, which is existing behaviour.*

## 4. The prose a future session reads

- [x] 4.1 Add the pitfall bullet to `CLAUDE.md` immediately after *Retrieval settings are profile and leave no residue*; verify it says the test failing is the signal to add the contract value, not to update the test
- [x] 4.2 Correct `docs/design.md` § 5 step 2 from "a config switch" to a contract switch, and append the corpus-coupled note to the *Summarization layer* deferred entry; verify a grep for `summar` across `docs/`, `CLAUDE.md` and `README.md` leaves no other place describing the switch as infrastructure. *One further place found and corrected: `docs/handover.md`'s M3 table row also called it "a config switch, off by default". Everything else the grep returned describes the feature's timing or the domain model, not its layer.*
- [x] 4.3 Record in `docs/handover.md` that the baseline now states the corpus it was measured over and that one stating none is reported as not comparable; verify the evaluation-harness section says it where the baseline comparison is described

## 5. Verification and archive

- [x] 5.1 Run `pytest -q`; verify the full suite is green, which is the same gate CI runs. *479 passed, 2 skipped.*
- [x] 5.2 Run `openspec validate --all --strict`; verify it is clean, and check mechanically that each MODIFIED block reproduces every published scenario title, since validate does not exercise `specs-apply` and `archive` has no dry run. *17 passed, 0 failed. The title audit also confirmed no ADDED requirement collides with a published one and named the eight requirements left deliberately untouched.*
- [x] 5.3 Grep the changed files for the public-repo leak patterns; verify the identity string added to the baseline carries a model name and a library version and nothing else. *The only hits are the rule text in `CLAUDE.md` itself.*
