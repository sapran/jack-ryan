## 1. The rule in the published specs

- [x] 1.1 Establish by falsification that no spec is affected; verify by reading what each candidate actually asserts rather than by whether the change feels surface-shaped. *`service-adapter-boundary` is the only spec naming REST, and its requirements are about where rules live and how typed errors are translated — this change moves neither. `extraction-quality-gate` requires the CLI and REST to report `read_as` "using the same name and the same words", which this makes structural rather than duplicated: satisfied more strongly, falsified not at all. A MODIFIED block for either would reproduce it byte-identically; cut. Recorded in the proposal's Impact.*
- [x] 1.2 Confirm no spec asserts a response's field order before accepting the one behaviour change; verify by searching for it rather than assuming its absence. *Grepped `openspec/specs/` for order, serialisation and rendering: the only hit is `casefile-lifecycle`'s reference-resolution order, which is about which handle is tried first, not about JSON keys.*

## 2. The shared module

- [x] 2.1 Create `src/jackryan/rendering.py` with `render_casefile`, `render_document` and `render_hit`; verify it imports nothing that would make it a cycle. *Both adapters already imported `read_as` from `ingestion.quality_gate` and the three domain objects from `storage.port`; the new module imports the same two and nothing else, and neither adapter imports the other.*
- [x] 2.2 Give `render_hit` a `round_scores` parameter rather than writing the seventeen fields twice; verify it is keyword-only, so neither call site reads as an accident. *Keyword-only. The CLI passes `True` because a terminal table of unrounded floats is unreadable; REST passes `False` because a remote caller may want the value it was given.*
- [x] 2.3 Give `render_document` the nine shared fields and no flags; verify the alternative was considered and rejected in writing rather than by omission. *Recorded in the design: a five-flag version would be an interface as wide as the body it hides, which is the shape this change removes.*
- [x] 2.4 State in the module's own docstring why the agent surface is excluded; verify the reason is the risk of someone "finishing the job" rather than a note that it is different. *Written that way — `interfaces/mcp` uses `document_id`, collapses corpus values to one line, and carries no chunk summary, which `test_mcp_fencing.py` pins.*

## 3. The adapters

- [x] 3.1 Point both adapters at the shared functions, keeping their own named functions; verify the three names three tests import by hand still resolve. *`cli._render_document`, `server.serialize_document` and `server.serialize_hit` are still real functions. `test_text_source.py` imports all three and reports `render.__module__` on failure, so aliasing them would have cost that test its ability to say which surface disagreed.*
- [x] 3.2 Drop the now-unused `read_as` import from both adapters; verify it is genuinely unused rather than merely rarer. *The CLI's remaining occurrence is `item['read_as']`, a dict key, not the function. REST had two real calls, both now inside the shared module.*
- [x] 3.3 Confirm the key **set** and every value are unchanged on both surfaces; verify by constructing a document and comparing against the recorded pre-change order. *Same thirteen keys on REST, same values. Order changed: `casefile_id` third → tenth, `summary`/`summary_by` after `created_at`. Stated in the proposal.*

## 4. Verification

- [x] 4.1 Assert the two surfaces agree on all nine shared fields, and that each keeps its own extras; verify the test asserts the divergence too, so collapsing it fails rather than passing quietly. *Asserted asymmetrically, and 6.3 corrects this line's original claim of symmetry: REST's four extras are unconditional and exactly pinned; the CLI's four are conditional and none fires on this corpus, so that side pins "added nothing here". Recorded rather than closed — see the parked note.*
- [x] 4.2 Assert nothing but the rounding separates the two hit renderings; verify it compares everything-except rather than field by field, so a field added to one and not the other fails here. *Written as a dict comparison with the two score keys removed, plus a key-set equality.*
- [x] 4.3 Watch both fail: reintroduce drift on a shared field, and stop the CLI rounding. *`AssertionError: REST disagrees about characters`, and `assert 0.03278688524590164 == 0.032787`.*
- [x] 4.4 Record that both tests would have passed before this change; verify the distinction is stated rather than left for a reviewer to notice. *They describe a property that was already true by coincidence — what changed is that it is now true by construction. In the design's Risks, because running them green proves nothing about this diff.*
- [x] 4.5 Run the full suite and confirm the count rises by exactly two. *695 → 697, 3 skipped, on a clean run. **Read that as "697 non-skipped tests exist", not "697 pass on every run":** a reviewer on the same commit measured 696 passed / 1 failed, and the failure was `test_a_timeout_kills_the_whole_converter_tree_not_just_the_launcher` — a load-sensitive pre-existing flake whose code is byte-identical to `develop` on this branch, and which CI's pytest gate passed. The count claim was written from a green run without noting that the run is not always green, in a change about verification rigour. See the re-measured note in `docs/implementation-notes.md`.*
- [x] 4.6 Re-apply and re-verify after a `git checkout` discarded the working edits mid-run; verify the shared renderers are actually wired rather than trusting a green suite. *They were not, for one run: `git checkout <file>` to undo a mutation also reverted the uncommitted change, and the suite stayed green at 697 because the two independent copies still agreed — which is exactly what the new tests assert. Caught by reading `git status`, re-applied, and confirmed by grepping both adapters for the shared calls and by the diff removing 101 lines.*
- [x] 4.7 Run `openspec validate --all --strict`; verify a change directory with no `specs/` is accepted, since that is unusual here. *It is **not** accepted by default — `Change must have at least one delta`. A delta-free change needs `.openspec.yaml` carrying `skip_specs: true`, and the marker is ignored unless the file is otherwise valid metadata: `schema: spec-driven` and `created:` are both required, which the first attempt omitted and which produced a second, clearer error. 20/20 once both were present. This is the first change in the repository to use `skip_specs`; the only other `.openspec.yaml` in the archive carries the two metadata keys and no marker.*

## 5. Review round

- [x] 5.1 Fix the rounding assertion, which could only fail in one direction; verify by making REST round too and confirming the old assertion missed it. *`assert from_cli["score"] == round(from_rest["score"], 6)` holds whether or not REST rounded, because rounding an already-rounded value changes nothing. Flipping `serialize_hit` to `round_scores=True` — destroying the one difference the parameter exists for, and silently truncating what a remote caller asked for — left **55 tests green** across `test_result_shape.py`, `test_rest.py` and `test_reranking.py`. Both surfaces are now asserted against `hit.score` itself, and the fixed test fails on that mutation with `assert 0.032787 == 0.03278688524590164`.*
- [x] 5.2 Cover `rerank_score`, the second rounded field; verify nothing in the suite pinned it before. *Its **rounding** was pinned nowhere on the two human surfaces, so `scored()` returning it unrounded while still rounding `score` would have passed. Stated carefully because the first version of this evidence said "pinned nowhere in the suite" and then listed the three places it appears — the field itself is asserted on the agent payload and at the service level; only its rounding on the CLI and REST was not. A hit now carries one via `dataclasses.replace`, and the test asserts the rounding is not a no-op on that value, or the four assertions would agree for a reason unrelated to rounding.*
- [x] 5.3 Make the divergence check exact rather than a subset; verify the docstring's claim to assert it "in both directions" is now true. *`set(from_rest) - set(shared)` compared for equality, so a fifth REST-only field fails rather than passing. The CLI side asserts an empty difference on a corpus ingested without a summariser.*
- [x] 5.4 Confirm behaviour preservation was established by measurement, not by reading. *A reviewer built a differential probe over 2 casefiles × 144 document variants × 2304 search-hit variants comparing the parent's six renderers against the new ones: zero value differences, zero keys gained or lost, and the REST document key order as the single divergence — exactly as claimed. Also grepped `docs/`, `openspec/` and `README.md` for a sample payload showing the old order: none, so nothing tracked went stale.*
- [x] 5.5 Note what this change makes stale elsewhere without fixing it. *`tests/test_text_source.py:254` says "Four renderers, one key, one vocabulary" while iterating three. Already inaccurate before this change, so left alone under the drive-by rule — but two of those three now share an implementation, which makes the count read worse than it did.*

## 6. Pre-merge review round

- [x] 6.1 Have the fix commit reviewed before merging; verify the reviewer reproduced the mutation rather than reading the assertion. *Verdict MERGE. It reproduced `round_scores=True` on REST via an out-of-process pytest plugin and got the exact reported failure, then probed four further directions — CLI stops rounding, `rerank_score` left unrounded, an extra REST-only field, an extra CLI-only field — all caught.*
- [x] 6.2 Correct the `rerank_score` overclaim in both places it was written; verify the narrower claim rather than deleting the wider one. *"Covered for the first time anywhere in the suite" is false — the field is asserted on the agent payload in `test_result_shape.py` and at the service level in `test_reranking.py`. What was uncovered is its **rounding on the two human surfaces**, which is what this delivers. The evidence line in 5.2 contradicted itself inside one sentence and now does not.*
- [x] 6.3 Make the docstring honest about the asymmetry it claimed was symmetric; verify by asking what the CLI-side assertion would catch. *It claimed the divergence is asserted "in both directions". REST's four extras are unconditional and genuinely pinned; the CLI's four are conditional and none fires on this corpus, so that side pins "added nothing here". Deleting the `found_at` and `children` blocks would keep the test green. Docstring rewritten to say so, and the gap parked rather than closed — the honest fix is a CLI-level test of an expanded document, not a wider fixture here.*
- [x] 6.4 Settle whether the failing legacy-office test is this change's fault; verify by comparing the code rather than by trusting either the flake note or the reviewer. *`git diff origin/develop..HEAD -- src/jackryan/ingestion tests/test_legacy_office.py` is empty, so any failure there is a failure on `develop`. CI's pytest gate passed on this commit. The reviewer called it "reproducible, not flaky" and I had seen it pass in isolation minutes earlier — both true, because the race widens under load. The parked note now says so instead of quoting a rate.*
