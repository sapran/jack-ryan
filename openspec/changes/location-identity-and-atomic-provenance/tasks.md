## 1. Reproduce before changing anything

- [x] 1.1 Reproduce the interrupted-ingest blocker: interrupt between the document write and the location write, then ingest identical bytes from a second root; show the verdict becoming `complete` with the first root absent. *Reproduced on `85c24a4` before any edit: `flag=True rows=0 verdict='unknown'` at rest, then root B gave `verdict='complete' total=1` with root A absent and the outcome reported as `new`.*
- [x] 1.2 Reproduce the same-physical-file identity defect: ingest a folder, then its nested file directly, then the containing subfolder; show two recorded locations rendering as one path and a `new` outcome for step 2. *Reproduced: 2 rows, 1 distinct rendered path, step 2 `new`, step 3 `known`.*
- [x] 1.3 Write the deltas, scoped by falsification against every published requirement, and validate before implementation. *Three `MODIFIED`, one `ADDED`; 23 published scenarios reproduced verbatim and 3 added, checked by parsing both files rather than by reading. `openspec validate --all --strict` → 19 passed, 0 failed, before any code changed.*

## 2. A location is a path

- [x] 2.1 Schema rung 10: `document_observations` keyed `(document_id, location_path)`, cascading with its document; copy `document_locations` across, joining each pair and collapsing duplicate places to the earliest timestamp. *`SCHEMA_VERSION` reads 10 from `_STEPS` = (5…10). Mutation 6 keeps the pair in the key and reddens three guards; mutation 7 drops the copy and mutation 8 groups by the pair instead of the path, each reddening the carry-across test.*
- [x] 2.2 Leave `document_locations` untouched and unwritten, with the rung comment saying it is retained deliberately. *Asserted directly: the carry-across test checks all three prior rows still exist afterwards.*
- [x] 2.3 Add `document_observations` to the destructive-step guard's evidence tables, so a later rung cannot delete from it. *Added, and `document_locations` with it — review pointed out that the "kept deliberately" instruction was comment-only, which this module's own docstring argues is a rule a later change breaks without noticing.*

## 3. One write

- [x] 3.1 Replace `upsert_document` with `store_document`, taking the observation as a required parameter and writing both in one transaction. *Explicit `BEGIN` and rollback, following `replace_chunks`. Mutation 1 commits the document before the observation and mutation 2 removes the rollback; both redden the atomicity guard.*
- [x] 3.2 Remove `upsert_document` from the port and the store rather than deprecating it; migrate all three test callers. *No caller remains. `test_the_port_cannot_store_a_document_without_saying_where` asserts by signature that no way to store a document without its observation exists, and that neither parameter has a default.*
- [x] 3.3 Verify a failure partway leaves neither the document nor its observation. *`test_a_document_is_not_stored_without_its_observation`, via a `BEFORE INSERT` trigger that aborts only that statement. The first version of this guard dropped the table instead, which fails the identity lookup before the document is ever inserted — it passed while never reaching the write it names, and mutation proving is what caught it.*

## 4. Derived completeness

- [x] 4.1 Derive the verdict from `min(first_seen_at) <= created_at`, in one place used by the service and by both listing surfaces. *`Document.locations_are_whole`. The service asked the fetched set at first, which spelled the rule twice; review flagged it and the service now asks the document. Mutations 3 and 4 each redden it.*
- [x] 4.2 Stop reading and writing `documents.locations_recorded`; record in the rung comment why the column stays. *Neither written nor read; the ladder is additive so the column remains, with the rung saying nothing may start reading it again.*
- [x] 4.3 Alias `MIN(first_seen_at)` on the listing query, and derive the marking threshold from the same rule rather than in each adapter. *One `_OBSERVATION_ALIASES` fragment, selected by the four queries a caller resolves a document through. `ancestors` and the mention-carrier listing deliberately omit it, the latter because its missing count is a separately parked finding this change was told to leave standing. Mutation 11 drops the aliases from a resolution query and reddens the guard.*
- [x] 4.4 Pin that a new document's `created_at` equals its first observation's `first_seen_at`, so a refactor calling the clock twice fails loudly. *`test_a_new_documents_record_begins_when_the_document_does`, asserting equality rather than ordering, and now also that the document reads whole through `resolve_document` by short id and by full id — review pointed out the listing was the only path covered.*

## 5. Proof

- [x] 5.1 A regression test for the retry transition, not only the immediate zero-observation state. *`test_a_later_root_cannot_claim_the_history_of_an_interrupted_ingest`: unknown at rest, unknown after root B with root B recorded and root A absent, `new_locations` empty, and still unknown after a third root.*
- [x] 5.2 A regression test for one file reached through two roots, and for two roots holding one path each staying two locations. *`test_one_file_reached_through_two_roots_is_one_location` and `test_two_roots_holding_one_path_each_are_still_two_locations`. The second is what stops the fix for the first from merging two custodians.*
- [x] 5.3 A test that the port offers no way to store a document without its observation. *By signature inspection, because what must not exist is the option — a call-based check would pass against a defaulted parameter.*
- [x] 5.4 Mutation-prove every new guard. *11/11 red with the symptom each test is named for, measured on the final tree against a 59-test green control, every mutation verified to import from inside the copied tree. Re-measured after the verdict rule was unified, because one anchor had ceased to exist and the earlier table no longer described the shipped code.*
- [x] 5.5 Verify through the real CLI and a real `serve-mcp` stdio process, on disposable data. *One file reached three ways: `first`, `known`, `known`; CLI shows `locations 1`; the MCP listing row carries no `locations` and `case_read_document` no `locations` block, both correct for one place with a whole record. Two custodians: `locations 2`, `recorded=complete total=2 truncated=False`, both paths shown; `case_cite` names the first observed. Re-run after the last refactor rather than cited from the earlier pass.*
- [x] 5.6 Carry a real pre-change store forward and show its records arriving in the new table with duplicates collapsed and no location invented. *`test_existing_location_records_are_carried_into_observations`, from a store hand-built in the rung-9 shape holding one place twice and one place once. Its fixture orders the pair and the timestamp in disagreement on purpose: with them agreeing, grouping by the pair also kept the earliest and the test passed while proving nothing.*
- [x] 5.7 Gates. *pytest 811 passed, 3 skipped; `openspec validate --all --strict` 19 passed, 0 failed; `gitleaks detect` no leaks; `docker compose build` exit 0, both images built. The build failed once first on a `files.pythonhosted.org` read timeout — recorded because a clipped `tail` of that run looked like a pass, and the stale image beside it would have made the mistake hard to see.*
- [x] 5.8 Update the task record with the evidence, leaving task 5 awaiting independent PM acceptance.
