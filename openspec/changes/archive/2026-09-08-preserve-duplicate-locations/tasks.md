## 1. The rule in the published specs

- [x] 1.1 Write four `MODIFIED` deltas — `document-ingestion`, `document-hierarchy` (two requirements), `untrusted-content-boundary`, `mcp-tool-surface` — reproducing every existing scenario verbatim and retitling none. *26 published scenarios reproduced, 13 added (2 and 8 in the two `document-hierarchy` requirements, 4 in `document-ingestion`, 6 each in `mcp-tool-surface` and `untrusted-content-boundary`; the added ones grew to 13 when the root-qualification and container-reingest scenarios landed). Checked mechanically by parsing both the published spec and the delta and diffing the scenario titles per requirement, rather than by trusting `validate` — which does not exercise `specs-apply`.*
- [x] 1.2 Establish by falsification that no delta is owed for `ingestion-coverage`, `schema-migration`, `storage-seam` or `hybrid-search`, re-reading each published text rather than judging by topic; record the table in the proposal's `## Impact`. *Thirteen published texts re-read this session. The near miss was `document-ingestion:127-128` — a content-routed file "keeps the filename it carries on disk" — which is not falsified: the stored name is still one the file carries on disk, the one at the location observed first. Table in `## Impact`.*
- [x] 1.3 `openspec validate --all --strict` before any code is written. *19 passed, 0 failed — 18 published capabilities plus the change itself. The plan predicted "18/18"; the tool counts the change too.*

## 2. Schema step 9

- [x] 2.1 Append one `_Step` creating `document_locations` with a composite primary key and `ON DELETE CASCADE`; verify no separate index is added and `SCHEMA_VERSION` stays derived. *`SCHEMA_VERSION` reads 9 from `_STEPS` = (5, 6, 7, 8, 9). Mutation 10 dropped the cascade and reddened `test_deleting_a_document_deletes_its_locations`.*
- [x] 2.2 Add `documents.locations_recorded` with a constant default of 0; verify `_SCHEMA` and `BASELINE_DOCUMENT_COLUMNS` are untouched and the frozen-baseline test still passes. *Neither touched; `test_the_frozen_baseline_is_frozen` green throughout.*
- [x] 2.3 Verify `test_no_step_is_destructive` and the baseline-equals-ladder test both pass with the new rung. *Both green. One pre-existing test did break and was re-anchored rather than relaxed: `test_an_older_store_gains_the_ingest_run_record` derived its store's version as `max(to_version < SCHEMA_VERSION)`, which was the `ingest_runs` step only while that was the top of the ladder — so any new rung made it build a store already carrying that version and skip the step it exists to exercise. It now keys on the step whose statements name `ingest_runs`.*

## 3. The port speaks in domain objects

- [x] 3.1 Add `DocumentLocation` and `DocumentLocationSet` as frozen dataclasses, with `truncated` derived rather than stored. *Both frozen; `truncated` is `len(locations) < total`.*
- [x] 3.2 Add `locations_recorded` and `location_count` to `Document`, both defaulted, with the comment saying why `False` is the honest default. *`locations_recorded` is read directly because it is a real column every query selects; `location_count` takes the `child_count` guard because only the listing query aliases it.*
- [x] 3.3 Declare `record_document_location` and `document_locations` on `StorePort`. *Both declared, both returning domain objects, satisfying `storage-seam`'s scenario at `:42`.*

## 4. The store stops overwriting, and records

- [x] 4.1 Remove `filename` and `containment_path` from `upsert_document`'s `DO UPDATE SET`, leaving the comment that states the rule; verify the trailing comma moved to the new last clause. *`parent_id = excluded.parent_id` is now the last clause and lost its comma. Mutations 1 and 2 restored each removed clause and both reddened `test_a_copy_found_later_does_not_overwrite_the_first_location`.*
- [x] 4.2 Add `locations_recorded` to the insert list and **not** to `DO UPDATE SET`; verify a reingest cannot raise a migrated document's flag. *Mutation 3 added it to `DO UPDATE SET` and reddened `test_a_document_that_predates_the_record_stays_unknown_across_a_reingest`.*
- [x] 4.3 Implement `record_document_location` with `INSERT OR IGNORE` returning whether the row was new; verify `OR REPLACE` would report every reingest as a discovery. *Mutation 4 (`OR REPLACE`) and mutation 5 (return `True` unconditionally) each reddened `test_a_same_location_reingest_records_no_new_location`.*
- [x] 4.4 Implement `document_locations` with a total ordering and a bound; verify the count is of the whole set, not of the page. *Ordered by `first_seen_at, source_root, containment_path`; `total` is a separate `COUNT(*)` with no `LIMIT`.*
- [x] 4.5 Add the `location_count` correlated subquery to `list_document_page` beside the existing `child_count` one. *Mutation 7 forces the subquery to `SELECT 0` and reddens exactly the two listing-marking guards. Its first form commented the subquery out, which truncated the whole implicitly concatenated statement and reddened fifteen guards on an sqlite `OperationalError` — a mutation that reddens for the wrong reason establishes nothing, so it was redone.*

## 5. The verdict, and the vocabulary

- [x] 5.1 Add `MAX_DOCUMENT_LOCATIONS` and the six vocabulary constants. *Twenty, with no continuation, argued in `design.md` Decision 6.*
- [x] 5.2 Add `IngestOutcome.location`, defaulted to empty for an outcome that never got that far. *Also `location_path`, the already-joined followable path, so the report and the query path cannot spell one location two ways.*
- [x] 5.3 Add `IngestReport.new_locations`, derived, and verify it is **not** folded into `limitations`. *Mutation 6 folded it in and reddened `test_a_copy_at_a_new_location_does_not_make_the_run_incomplete`, which asserts `complete is True` and `limitations == []` beside the discovery.*
- [x] 5.4 Add `DocumentLocationRecord` with `also_found_at`, `truncated` and the single-sourced `note`. *`also_found_at` excludes the earliest recorded location **by position**, not by comparing containment paths — mutation 14 made that comparison and was the one mutation that came back GREEN, because the only fixture that can see the difference is the cross-root one, where both locations share the relative path and such a comparison discards both rather than one. The test now asserts `also_found_at` and the mutation reddens.*
- [x] 5.5 Record the location in `_ingest_work` after the upsert and before the chunks, and decide the four-value verdict with `unknown` tested before the store's answer. *Ordering corrected after the first attempt landed the block below `replace_chunks`, contradicting its own comment. Mutation 9 forced the verdict to `complete` and reddened the predates-the-record guard.*
- [x] 5.6 Add `IngestionService.document_locations`, resolving through `resolve_document`. *Casefile scoping and 8-character prefixes inherited rather than restated.*

## 6. Every surface

- [x] 6.1 Add `location` to the shared `render_report` so neither human surface can drift. *`ingestion-coverage:79` stays true by construction.*
- [x] 6.2 CLI: mark a multi-location document in a listing, report the full record in `document show` and its `--json` form, and print the new-locations block below the limitations one. *Observed on the real binary: `locations 2`, one `also found at /…/custodian-b/ledger.txt`, and the banner "Already in this casefile, and now recorded at a location it had not been seen at".*
- [x] 6.3 REST: return the record on the document detail route only, leaving `serialize_document` shared and unwidened. *Detail route returns `found_at`, `locations_recorded`, `locations`, `also_found_at`, `locations_truncated`, `locations_note`; the listing route is unchanged.*
- [x] 6.4 MCP: emit a `locations` provenance block only when it says something; mark a listing row; extend the two tool descriptions. *Through a real `serve-mcp` stdio process: the listing row reads `locations = 2` and the provenance block reads `{"recorded": "complete", "also_found_at": ["/…/custodian-b/ledger.txt"], "total": 2, "truncated": false}`. Mutation 11 emitted it unconditionally and reddened the negative twin.*
- [x] 6.5 Verify `case_search`, `case_cite` and `case_get_passage` are untouched. *None edited. `case_cite` was driven over stdio and named `ledger.txt` — root A's path, the first observed — after root B's copy had been ingested.*

## 7. Proof

- [x] 7.1 Ten tests in `tests/test_document_locations.py`, including the negative twin that stops the provenance block being emitted unconditionally. *Seventeen, not ten: the cross-root case and the container-reingest case were added after the root-qualification decision, a migrated-document verdict guard beside them, and four more from the review round — the hidden recorded location, the listing threshold, the flag-without-rows verdict and the shared renderer.*
- [x] 7.2 One test in `tests/test_migrations.py` for a document carried forward from a v4 baseline, staying `unknown` across a reingest. *`test_a_document_carried_forward_reports_its_locations_as_unknown`: the flag is `False`, no location is invented, and recording one afterwards does not raise it.*
- [x] 7.3 Eleven mutations, each red with the symptom its test is named for, against a green unmutated control. *Eighteen, after the review round; number 11 was dropped as superseded by 19 on the current anchor. Control 40 passed; every mutation verified to import from inside the copied tree (uv's editable `.pth` otherwise pins the original `src` and every mutation reports GREEN). Two came back GREEN on their first pass and each exposed a real gap rather than a redundant check — the positional-exclusion mutation, because only the cross-root fixture can see it, and the shared-renderer mutation, because nothing asserted it at all. Both fixed and re-run red. Final: 18/18.*
- [x] 7.4 Gates: `pytest -q`, `openspec validate --all --strict`, `gitleaks detect`. *On `94304c8`, before the review round: 773 passed, 3 skipped; 19 passed, 0 failed (18 specs plus the then-active change); no leaks across 71 commits. Superseded by the figures in task 8.12, measured on `940e0d3` after the review fixes.*
- [x] 7.5 End-to-end synthetic proof under a temporary data directory, including through a real `serve-mcp` stdio process and a v4 store carried forward. *Two custodian roots each holding `ledger.txt` at their top level: one document, two locations, `first` then `new`, `complete` true. Reingest → `known`, still two. A zip of two identical entries → two documents, one location each. Reingesting the container → `known` only, no row growth. The unknown verdict shown on CLI human, CLI `--json` and REST with one identical sentence. A v4 store gained both objects, kept its text, invented no location, and left a 118,784-byte `old.db.v4.bak`.*
- [x] 7.6 Tick `docs/functional-review-todo.md` item 5 with its evidence. *Done, with the commits, the three gate results and the mutation table.*

## 8. Review round

Two reviewers on `d14a6b8`, correctness and silent-failure. Every finding was
reproduced before being acted on; none was taken on the reviewer's word.

- [x] 8.1 **Blocker — a migrated document's only recorded location reached no surface.** `also_found_at` dropped `recorded.locations[0]` on the invariant that the earliest row is the one the document's own `containment_path` came from, which holds only for a document the new code created. Reproduced as a failing test first. Fixed by disclosing `observed_at` — every recorded location, earliest first, each carrying its root — so nothing is excluded and no invariant is needed to decide what to exclude. Mutation 14 restores the positional drop and reddens five guards.
- [x] 8.2 **The same root cause made the reversed-order acceptance check fail on the shipped surface.** The document's own path is relative to a root no column holds, so a partial list rendered a different visible set depending on which dump was ingested first — forward showed `custodian-b/ledger.txt`, reverse showed `custodian-a/ledger.txt`. Fixed by the same change and re-verified through the CLI: both orders now show the same two root-qualified locations, `same set of locations: True`.
- [x] 8.3 **The listing threshold missed a migrated document's first recorded location.** `> 1` marks one location too late where there is no own-location row. Now `> (1 if locations_recorded else 0)`, using a flag the listing query already selects. Mutation 16.
- [x] 8.4 **A raised flag over an empty record rendered as an ordinary single-location document.** The two writes are separate commits, so a death between them is reachable. The verdict now requires a row as well as the flag, so the contradiction reads unanswerable and carries the caveat. Mutation 17.
- [x] 8.5 **`render_report` could not name an expansion's new location.** `location_path` never reached the shared renderer, and an expansion's `path` is a scratch file already deleted — so the CLI's text printed a location the JSON and REST forms could not express. Added, and guarded by a test that also asserts the scratch path no longer resolves, which is why the field exists. Mutation 18 was GREEN on the first pass and is now red.
- [x] 8.6 **The provenance block keyed on a copy of the word `complete`.** Now keyed on the service-computed `note`, which is non-empty exactly when the record cannot answer, so the adapter no longer restates the service's vocabulary. Mutation 19 still reddens the negative twin.
- [x] 8.7 **The tool description told the model what a location count means.** "which is shared custody or distribution" is a conclusion the count cannot support — an operator ingesting one dump from two paths produces it too. Replaced with what the count is, leaving the inference to the analyst, in the register the surface's own epistemics use. `case_read_document`'s "every other location" also dropped its overclaim.
- [x] 8.8 **The port docstring and design Decision 5 claimed an expansion always has exactly one location.** False under this change's own key: an identity path carries no root, so one archive ingested from two dumps is one document with two locations. Both corrected, with the reason the plan's contingency would have lost the second custodian for every archived file.
- [x] 8.9 **Spec truth.** The published provenance requirement was unqualified while only `case_read_document` carries the disclosure. Narrowed to a tool returning a document's own text, with the exclusion of the passage and citation tools argued in the same paragraph, in both the published spec and the archived delta — so code and spec agree in the change that ships them.
- [x] 8.10 **Findings recorded rather than fixed**, each with its reasoning, in `docs/implementation-notes.md`: a first-observed filename can describe a later reading (identical bytes under two suffixes route to different extractors); the CLI prints location paths raw, consistent with its three existing raw sites; and two pre-existing cleanup tests snapshot the machine-wide temp directory, so any second jackryan process fails them.
- [x] 8.11 **The suite's flake was established, not assumed.** Making the swallowed `rmtree` error loud gave five clean full runs, so removal is reliable and the surviving directory was never that ingest's. A loop creating and deleting `jackryan-expand-*` beside the suite reproduces the failure on demand. Untouched by this change; `git diff develop...HEAD` includes neither test.
- [x] 8.12 Gates. CI's three are pytest, gitleaks and a Docker build — `openspec validate` is this project's own fourth check, not one of CI's, and an earlier draft of this evidence called the trio "the three gates" while never having built the image. On `3673087`: 804 passed, 3 skipped; no leaks; `docker compose build` built `jackryan:latest` for both services; 18 passed, 0 failed on openspec. 777 passed on `940e0d3` before the merge. 18/18 mutations red against a 40-test green control.
