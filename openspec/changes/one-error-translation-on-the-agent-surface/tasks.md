## 1. The rule in the published specs

- [x] 1.1 MODIFY `service-adapter-boundary`'s *The service layer raises typed errors that adapters translate*, reproducing both published scenarios verbatim and adding one for the agent surface; verify the new scenario asserts *one* translation rather than merely that translation happens, since the eight copies satisfied the latter. *Both published scenarios reproduced byte-for-byte, checked mechanically. The new scenario is phrased as "every tool translates through the same one", which the eight copies would have failed.*
- [x] 1.2 MODIFY `mcp-tool-surface`'s *A failing tool returns a typed payload rather than raising*, reproducing its published scenario verbatim and adding one for a failure raised after a tool's opening calls; verify it is written about where in the tool the failure happens, not about which tool. *Written as "however far through its work a tool fails", so it covers the other four tools that build a payload outside the old block rather than naming `case_get_passage`.*
- [x] 1.3 Establish by falsification that `mcp-surface-profiles` and `analyst-pack` need no delta; verify profiles prune by `Tool.name`, which comes from the explicit `name=` argument and cannot be changed by a decorator, and that the pack names tools without describing how they fail. *Read both. Pruning runs after definition over `_defined_tool_names`; `Tool.name` is the literal passed to `@server.tool`. A MODIFIED block for either would reproduce it byte-identically, which the delta guidance says to cut. Recorded in the proposal's Impact.*

## 2. The translation

- [x] 2.1 Add `returns_error_payload` to `interfaces/mcp/errors.py`, wrapping the whole tool; verify it is `async def` and carries `functools.wraps`, and that the docstring states why each is load-bearing rather than merely that it is there. *Both stated with the failure they prevent, because neither is guessable from the call site and neither is caught by any other test.*
- [x] 2.2 Apply it to all eight tools **below** `@server.tool(...)`; verify the ordering, since reversed the SDK would register the undecorated function and the translation would never run. *Eight applications, counted. Ordering is bottom-up so the wrapper is what `server.tool` receives.*
- [x] 2.3 Remove the eight `try`/`except JackRyanError` blocks and de-indent their bodies; verify no tool keeps a second `except` and that the diff changes no payload. *Grepped: no `except`, no `from_exception`, no `error_payload` left in `server.py`. Diff read in full — every hunk is a de-indent or a decorator line.*
- [x] 2.4 Drop the now-unused `JackRyanError` and `error_payload` imports; verify `error_payload` had no other caller before removing it, rather than assuming the grep that found it dead was complete. *`error_payload` has no caller in `src/` or `tests/`; it was dead before this change and is now unreferenced from `server.py` entirely. It stays exported from `errors.py`, which is where `from_exception` builds on it.*

## 3. Verification

- [x] 3.1 Assert the advertised input schema of every tool; verify the assertion names each tool's real parameters rather than checking that a schema merely exists, since a zero-property schema is still a schema. *Eight tools, parameters listed explicitly, plus an equality check on the advertised set so a new tool cannot be added without extending the assertion.*
- [x] 3.2 Watch 3.1 fail: remove `functools.wraps` and confirm the reported symptom is the advertised parameters, not an import or a crash. *`AssertionError: case_list_casefiles advertises {'kwargs', 'args'}` — the exact failure the decorator's docstring predicts. Restored and re-run green.*
- [x] 3.3 Watch the `async def` requirement fail: make the wrapper synchronous and confirm tool calls break. *12 failed, 7 passed in `test_mcp_surface.py`, every failure `UnexpectedToolError: Error executing tool ...`. Restored and re-run green.*
- [x] 3.4 Assert a typed failure raised after a tool's opening calls is still a payload; verify it is reached through the real service object the server closed over, not a re-implementation of the tool. *`monkeypatch.setattr(context.search, "passage_window", refuses)` on the same `context` the server holds, so the assertion exercises the shipped tool body.*
- [x] 3.5 Watch 3.4 fail: restore the old narrow `try` on `case_get_passage` alone and confirm the tool raises rather than returning a payload. *`UnexpectedToolError: Error executing tool case_get_passage` — the tool raised, which is what `mcp-tool-surface` forbids and what the change fixes. Reversed and re-run green.*
- [x] 3.6 Run the full suite; verify the count rises by exactly the two new tests and that nothing was skipped into passing. *689 passed / 3 skipped before, 691 passed / 3 skipped after. Same three skips, which are the opt-in live-endpoint tests.*
- [x] 3.7 Read the whole diff before committing rather than trusting the green suite; verify no mutation from 3.2, 3.3 or 3.5 survived. *Read hunk by hunk. `errors.py` is the decorator plus two import names; `server.py` is eight decorator lines, eight removed blocks, one import line; the test file is two added tests and one import. No residue.*
- [x] 3.8 Run `openspec validate --all --strict` and re-run the mechanical title audit, since validate does not exercise `specs-apply` and `archive` has no dry run. *18 passed, 0 failed. The audit is scripted against the published specs: 2/2 and 1/1 scenarios reproduced, one addition each, no `Purpose` block, no colliding title.*

## 4. Review round

- [x] 4.1 Correct the `functools.wraps` claim in all six places it was written; verify against the SDK by running `Tool.from_function` rather than by re-reading the source. *The claim was wrong twice: the degraded schema is `['args', 'kwargs']` with both **required**, not "no parameters at all", and the structured output schema survives without `wraps` because the wrapper carries its own `-> dict[str, Any]`. The observed evidence was in the change's own mutation output — `advertises {'kwargs', 'args'}` — and the prose contradicted it. Fixed in `errors.py`, `CLAUDE.md`, `design.md`, `proposal.md`, `docs/handover.md` and the test docstring.*
- [x] 4.2 Add an inspection test over what the SDK actually registered; verify it fails on the decorator applied *above* `@server.tool(...)`, which is the ordering error no existing test could see. *Applied to `case_casefile_overview` alone — the one tool nothing calls — the full 691-test suite stayed green. The new test fails with `case_casefile_overview was registered undecorated`. This is the scenario task 1.1 published and 3.1 did not implement.*
- [x] 4.3 Assert `is_async` in the same test; verify the parameters test does not already cover it, rather than assuming one test covers both mechanics. *It does not: with a synchronous wrapper the parameters test **passes**, because the signature survives `wraps` regardless. The sync failure was caught only incidentally, by the eleven pre-existing tests that happen to call tools. The CLAUDE.md pitfall claimed otherwise and was corrected.*
- [x] 4.4 Assert each tool's `required` list beside its parameter names; verify the two answer different questions. *They do: giving `query` a default leaves the names unchanged and lets an agent search with no query. Names alone would have stayed green.*
- [x] 4.5 Narrow the `mcp-tool-surface` delta, which over-claimed; verify the published sentence matches what the code does. *"However far through its work a tool fails" reads as covering every failure, while the implementation covers typed errors only — `int(limit)` still raises `ValueError`. Reworded to say typed errors and to state why the scope stops there.*
- [x] 4.6 Move the parameters test to `# -- the surface --` and record the reviewers' out-of-scope findings in `docs/implementation-notes.md` rather than fixing them; verify each is genuinely pre-existing before parking it. *Five parked, each checked against `develop` first. None is introduced by this change.*
