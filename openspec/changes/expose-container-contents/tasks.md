## 1. The rule in the published specs

- [x] 1.1 MODIFY `document-hierarchy`'s *Listing returns what was ingested, and reaches expansions on request*, reproducing its three published paragraphs and all four published scenarios verbatim and adding the container selection; verify the four surviving scenario titles are reproduced by parsing both files rather than by reading them, since a by-title omission is a validation ERROR that blocks archive. *Audited by a script replicating the validator's own by-title join: 4/4 reproduced, +4 added, 0 omitted, and all three published paragraphs plus all eight scenario bullet lines matched byte-for-byte.*
- [x] 1.2 MODIFY `mcp-tool-surface`'s *Reads are bounded, and truncation is explicit*, reproducing its five published paragraphs and all four published scenarios verbatim and extending the bound to a listing; verify the two counts are required to be separately named and the ordering required to be total, since those are the two claims the SQL has to satisfy. *4/4 scenarios reproduced, +2 added. The requirement has **four** published prose paragraphs, not five as the brief said; all four reproduced verbatim.*
- [x] 1.3 Establish by falsification that `storage-seam`, `service-adapter-boundary`, `mcp-surface-profiles`, `untrusted-content-boundary`, `extraction-quality-gate`, `casefile-lifecycle` and `analyst-pack` need no delta; verify by reading what each scenario actually asks rather than by whether the change feels in scope, and record the argument in the proposal's Impact. *All seven read in full. `storage-seam`'s "port hands back domain objects" is satisfied by `DocumentPage` rather than contradicted; `casefile-lifecycle`'s "listing is newest first" is about casefiles. Recorded per capability in the proposal's Impact, plus `document-hierarchy`'s own first requirement, whose "queryable in both directions" stays true.*
- [x] 1.4 Run `openspec validate expose-container-contents --strict`; verify it is clean before any code is written, since this is the one gate that runs before implementation. *Clean before implementation and again after: `openspec validate --all --strict` reports 18 passed, 0 failed.*

## 2. The value a paged listing returns

- [x] 2.1 Add `DocumentPage` to `storage/port.py` beside `CasefileStatistics` as a frozen dataclass carrying the documents, `total_matching`, `offset`, `limit`, `selection` and an optional `parent`; verify the field names say what they are, so a count cannot be read as the other count. *`total_matching` is named for the selection, not the page; the comment states why both are needed.*
- [x] 2.2 Make `truncated` and `continue_from` derived properties rather than fields; verify they cannot disagree with the documents actually carried, which is the reason `SearchHit.is_widened` and `Document.is_expanded` are properties too. *Both computed from `offset`, `len(documents)` and `total_matching`.*
- [x] 2.3 Use `truncated` and `continue_from` verbatim from `case_read_document`'s existing continuation contract rather than a new `has_more`/`next_cursor` pair; verify the two spellings match, so an agent meets one contract. *Same two names as `server.py`'s read payload.*

## 3. The port and the store

- [x] 3.1 Declare `list_document_page` on `StorePort` beside `list_documents`, documenting that `parent_id` takes precedence over `include_expanded` and that the count SHALL be computed under the same predicate as the page; verify the docstring states why, since nothing downstream detects a mismatched total.
- [x] 3.2 Remove `list_children` from `StorePort` and `SqliteStore`; verify its capability is wholly covered by `list_document_page(..., parent_id=...)`, which additionally scopes to the casefile and marks nesting — neither of which it did. *All six call sites migrated with their assertions unchanged, which is the proof of cover.*
- [x] 3.3 Add `_document_selection`, returning the predicate, the ordering and the selection name together; verify the page and the count cannot be built from different predicates, because one call site produces both. *Mutation-proved: widening the count's predicate to the whole casefile reddens nine tests.*
- [x] 3.4 Order the intake and `all` selections newest-first and a container's contents by containment path; verify each ordering ends in `d.id` so it is total, and record why an identifier is admissible here where the fused ranking forbids one. *Recorded in the docstring. **The tie-break is not exercised, and cannot be**: `created_at` is a per-document `now()` at microsecond resolution and two siblings cannot share a containment path, so ingestion produces no tie. Removing the trailing keys was mutated and left the suite green; the docstring now says so and says why they stay, rather than implying coverage.*
- [x] 3.5 Implement `list_document_page` as a count plus a narrow id subquery joined back for the rows; verify `extracted_text` never enters the sorter, which is the cost the page exists to avoid. *Measured on the real 2,068-document corpus: a page of 5 costs 26 ms and 2.9 M characters where the unbounded intake costs 420 ms and 64.5 M — 16x the time and 23x the text.*
- [x] 3.6 Repeat the `ORDER BY` on the outer query; verify by deleting it that the join does not preserve the subquery's order, which is the defect that makes consecutive pages overlap. *The claim in the brief is **false on this SQLite**, and the comment that asserted it has been corrected. `EXPLAIN QUERY PLAN` shows `SCAN chosen` driving the join and probing `d` by primary key, so the subquery's order is inherited; a sweep at 6, 20, 60, 200 and 600 children paged correctly with the line removed. It stays as insurance against a plan SQLite is free to change, which is what the comment now says. The **subquery's** `ORDER BY` is the load-bearing one and reddens three tests when dropped.*
- [x] 3.7 Alias `child_count` for every selection; verify a nested container inside a container's contents reports a non-zero count, which is the assertion `list_children` could not pass. *Asserted in tests, and confirmed on the real corpus: a container whose three children are each themselves containers, each marked `child_count=1`, and entering each returns exactly 1.*
- [x] 3.8 Reduce `list_documents` to a delegate with its signature unchanged; verify it and the paged method cannot come to disagree about what a casefile's documents are, and that its docstring names the paged method as what an adapter must call.

## 4. The service

- [x] 4.1 Add `DEFAULT_DOCUMENT_PAGE = 50` and `MAX_DOCUMENT_PAGE = 200` beside `MAX_FILE_BYTES`; verify both adapters import them rather than restating a bound, and that no second tighter adapter bound is added, since a listing row carries metadata rather than prose.
- [x] 4.2 Add `IngestionService.list_document_page`, clamping the limit and flooring the offset; verify it clamps rather than refuses, as every other bound on this surface does. *`limit=10_000` returns `MAX_DOCUMENT_PAGE`, asserted.*
- [x] 4.3 Resolve `parent_reference` through `resolve_document` only when it is non-empty; verify an omitted parent is treated as the default rather than as the mistake `resolve_document` refuses. *`resolve_document` raises `ValidationError` on an empty reference; the default path never reaches it.*
- [x] 4.4 Set `parent` on the returned page in the service; verify the casefile scoping lives here and nowhere else, and that a parent in another casefile raises `NotFoundError` before any child is queried. *Asserted, together with the other casefile still reporting itself empty.*
- [x] 4.5 Remove `IngestionService.list_children`; verify the six call sites are migrated rather than left with a shim. *No shim; a repository-wide grep for `list_children` now returns only the archived tasks file that first added it.*

## 5. The agent surface

- [x] 5.1 Give `case_list_documents` `parent`, `expanded`, `offset` and `limit`, forwarding them positionally; verify the call is positional because `anyio.to_thread.run_sync` forwards no keywords, which fails at the first paged call rather than at import.
- [x] 5.2 Rewrite the tool's description to teach that a `children` count is a container to enter and how to follow `continue_from`; verify it names `parent`, `total_matching`, `truncated` and `continue_from`, since an agent learns the payload from the description.
- [x] 5.3 Add `offset`, `total_matching`, `truncated`, `continue_from` and `selection` to the payload, leaving `total` meaning the entries in this payload; verify `total` is not redefined, since `listing_payload` computes it for every list-shaped tool. *`total` still comes from `listing_payload`; the whole-selection figure is the new `total_matching`.*
- [x] 5.4 Echo the resolved container under `parent` when one was asked for; verify an agent that passed an 8-character prefix can see which container it entered and cite it. *The journey test enters by `short_id` and asserts the echo carries the full id.*
- [x] 5.5 Add `_nothing_listed`, giving an empty page a message true of that page; verify an empty container does not read as an empty casefile, which is the same false negative as an empty result standing in for an unknown facet kind. *All three messages asserted, including the past-the-end one naming the selection's size.*
- [x] 5.6 Leave the per-row shape unchanged; verify a listing still carries no `summary`, `summary_by` or `fence_nonce`, and that `formatted` still holds exactly one line per row. *The existing fencing and M2 regression tests both still pass untouched.*
- [x] 5.7 Teach the tool in `INSTRUCTIONS` as step 3 and renumber the rest; verify the instructions still name `case_list_casefiles`, `case_search`, `case_cite` and `coverage` and still say content is not instructions. *`test_the_surface_teaches_the_method` passes unchanged.*

## 6. REST

- [x] 6.1 Give the documents route `parent`, `expanded`, `offset` and `limit`, spelled identically to the agent surface; verify every shared field name matches, since two spellings of one field is two contracts. *Asserted surface-against-surface on the same arguments, not each against the service.*
- [x] 6.2 Move the route onto `run_in_threadpool` with keyword arguments; verify keywords are safe here where they are a `TypeError` on the agent surface, and that the route no longer reads rows on the event loop. *The route previously called the service synchronously on the loop; it no longer does.*
- [x] 6.3 Leave `total`, `documents` and `serialize_document` unchanged; verify the nine shared fields and exactly four REST extras still hold, and that an existing caller's request is still valid. *`test_result_shape.py` passes untouched, and the default call is asserted to still list intake only. Note the two surfaces name a document's identifier differently on purpose — `id` in REST, `document_id` on the agent surface — which `rendering.py` records; the parity test compares the values, not the key names.*

## 7. The shipped analyst role

- [x] 7.1 Add container navigation to `analyst/role.md` as step 3 under Method and renumber the rest; verify the pack still names its tools and carries no vendor-specific text, and that a capability the role does not name is one the agent will not use. *`test_analyst_pack.py` passes unchanged.*

## 8. Tests

- [x] 8.1 Migrate the six `list_children` call sites in `test_containers.py`, `test_content_routing.py` and `test_rar_containers.py`; verify each assertion is unchanged, so the migration proves the new method covers the old one.
- [x] 8.2 Add the four new parameters to the advertised-parameter table; verify the table fails naming the tool if they are missing, rather than failing somewhere else.
- [x] 8.3 Page a five-entry archive at `limit=2` over three offsets; verify the concatenated ids equal the entries exactly, against an oracle the test builds itself rather than a second call to the code under test. *Six children rather than five — the brief's own figures disagreed between its §2 and §4, and six is what makes its `children: 6` journey true. Swept at `limit=2` (three full pages) and at `limit=4` (a short final page), both against `sorted()` of the fixture's own names.*
- [x] 8.4 Assert `total_matching`, `truncated` and `continue_from` on every page; verify a page past the end returns nothing while still reporting the selection's size, so it does not read as an empty container.
- [x] 8.5 Assert an over-large limit is clamped to `MAX_DOCUMENT_PAGE`; verify it clamps rather than raising.
- [x] 8.6 Assert a nested container appears among the children with a non-zero child count and can itself be paged; verify this is the assertion the removed `list_children` could not pass.
- [x] 8.7 Assert a parent in another casefile raises `NotFoundError`; verify nothing about its contents is returned.
- [x] 8.8 Mutation-prove the outer `ORDER BY` and the count predicate; verify the named test reddens for each and is restored, since a guard whose failure was not watched certifies nothing. *Four mutations run against a green control. **Two findings, neither visible by reading.** The fixture originally wrote its entries alphabetically, so insertion order and containment-path order coincided and dropping an `ORDER BY` left all twelve green — the creation order is now scrambled and the premise is asserted. And the count mutation did not fail, it **hung**: a total counted under a wider predicate keeps `truncated` true past the last document, so `while True` followed `continue_from` forever. Both sweep loops are now bounded and raise, naming the total. Final table: subquery `ORDER BY` RED (3 failed), count predicate RED (9 failed), outer `ORDER BY` GREEN and tie-breaker GREEN for the reasons in 3.4 and 3.6. A first attempt at the outer mutation deleted a trailing comma and reported a false RED — `TypeError: 'str' object is not callable` — which is a syntax break, not a guard.*
- [x] 8.9 Drive the agent journey through the tool surface — intake, then `parent` by 8-character prefix, then three pages, then read and cite a child; verify the citation names the containment path and no search ran. *The citation carries `bundle.zip/alpha.txt`.*
- [x] 8.10 Assert REST and the agent surface agree field by field on the same arguments; verify the default REST call still lists intake only.
- [x] 8.11 Run `pytest -q` and `gitleaks detect`; verify the suite rises by exactly the new tests and no test is lost. *725 passed, 3 skipped. Collected 716 on `develop` and 728 here: +12, exactly the new tests. `gitleaks detect` scanned 50 commits and found none, and the added lines carry no leak markers.*
- [x] 8.12 Page the real corpus read-only; verify `total_matching` matches the casefile's reported ingested count and that a container can be entered, ingesting nothing. *1 casefile, 2,068 documents, 1,792 ingested, 276 expanded. `total_matching == documents_ingested == 1,792`; a real container's 66 children swept in two pages of 50, each exactly once and none omitted. **Read strictly through a `VACUUM INTO` snapshot**: the live store is in WAL mode with a 19 MB WAL, and a plain file copy — byte-identical by md5 — silently reported one document fewer, because the WAL was not copied. The real `data/jackryan.db` was never opened by the code under test.*
